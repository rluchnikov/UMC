spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres?currentSchema=${spring.application.name}
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
